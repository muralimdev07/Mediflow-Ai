{
  "name": "mediflow-ai",
  "version": "1.0.0",
  "description": "Smart Hospital Queue and AI Triage System",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run dev"
  }
}
